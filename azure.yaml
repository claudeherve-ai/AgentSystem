name: agentsystem
metadata:
  template: aspire-samples
services:
  app:
    project: ./aspire/AgentSystem.AppHost/AgentSystem.AppHost.csproj
    host: containerapp
    language: dotnet
hooks:
  preprovision:
    windows:
      shell: pwsh
      run: |
        Write-Host "AgentSystem Aspire Deployment"
        Write-Host "=============================="
      continueOnError: false
