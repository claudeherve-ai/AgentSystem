[pytest]
# Auto-run async test coroutines via pytest-asyncio so integration tests that
# guard on credentials (e.g. tests/test_github_mcp_hard.py) skip cleanly instead
# of failing as un-awaited coroutines.
asyncio_mode = auto
testpaths = tests
