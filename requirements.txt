# AgentSystem - Dependencies
agent-framework-core>=1.0.0,<2
agent-framework-openai>=1.0.0,<2
# Pin openai: agent-framework-openai 1.x imports ResponseToolSearchCall which was
# introduced in 2.25 and removed again after 2.36. Pinning 2.25.0 the newest version that still exports it.
openai==2.25.0
python-dotenv>=1.0.0
pydantic>=2.0.0
aiosqlite>=0.20.0
apscheduler>=3.10.0
pyyaml>=6.0
azure-identity>=1.16.0
msal>=1.30.0
msgraph-sdk>=1.5.0
tweepy>=4.14.0
requests>=2.31.0
requests-oauthlib>=1.3.1
streamlit>=1.35.0
plotly>=5.22.0
pandas>=2.2.0
# ── FastAPI backend ────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
# ── Boil-the-Ocean capability tools (web/file/code/KB) ─────────────────────
# httpx is already a transitive dep of msgraph-sdk; pin it for web_search/web_fetch.
httpx>=0.27.0
# Web search backends
ddgs>=9.0.0
# Soft deps — file_reader and web_fetch lazy-import these and degrade gracefully:
markdownify>=0.11.6
beautifulsoup4>=4.12.0
pdfplumber>=0.11.0
python-docx>=1.1.0
openpyxl>=3.1.0
# ── Optional: code sandbox + self-observability ───────────────────────────
# The Docker code sandbox (tools/docker_sandbox.py) shells out to the Docker
# CLI and needs NO Python package. Telemetry (telemetry/) is dependency-free
# and exports spans in-process. The extras below are only needed if you wire
# an external trace backend; the app runs fully without them.
# opentelemetry-sdk>=1.27.0        # OTLP span export (OTEL_EXPORTER_OTLP_ENDPOINT)
# opentelemetry-exporter-otlp>=1.27.0
# langfuse>=2.0.0                  # Langfuse export (LANGFUSE_PUBLIC_KEY/SECRET_KEY)
# ── Optional: LLM-quality eval metrics (evals/) ───────────────────────────
# The offline eval suite (python -m evals.run) is dependency-free and is the
# CI gate. The ONLINE suite uses deepeval for LLM-judged metrics; each online
# task structurally self-skips when deepeval or LLM credentials are missing.
# deepeval>=1.0.0                   # online answer-relevancy metric (evals/metrics.py)

# -- Optional: Azure AI Search vector backend (PR4, tools/azure_search.py) --
# A Microsoft/Azure-native alternative to a standalone vector DB. When the SDK
# is installed AND AZURE_SEARCH_ENDPOINT/AZURE_SEARCH_API_KEY are configured,
# case memory (tools/rag_store.py) fans a semantic query out to Azure AI Search
# and merges the hits. Absent the SDK the feature is silently disabled and the
# local SQLite FTS5 + embedding store is used unchanged.
# azure-search-documents>=11.5.0

# -- Optional: Voice / OpenAI Realtime (PR5, tools/voice.py) ───────────────
# The durable-workflows feature (workflows/) needs NO new dependency — it
# rides on agent-framework-core (already pinned above). Voice is also fully
# fail-closed: the OpenAI Realtime adapter only activates when the `openai`
# package (a transitive dep of agent-framework-openai above) is importable
# AND VOICE_ENABLED=true with a real OPENAI_API_KEY. No extra install needed.